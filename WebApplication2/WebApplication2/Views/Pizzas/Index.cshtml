@model IEnumerable<WebApplication2.Pizza>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.pizzaName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.pizzaToppings)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.price)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.customerID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.pizzaName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.pizzaToppings)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.price)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.customerID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.pizzaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.pizzaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.pizzaID })
        </td>
    </tr>
}

</table>
